
<%= bootstrap_form_for :session, url: login_path, :html => {class: "col-sm-6 login-form"} do |f| %>
	<%= f.email_field :email %>
  	<%= f.password_field :password,label: "Password" %>
  	<%= f.submit "Log In" %>
<% end %>
