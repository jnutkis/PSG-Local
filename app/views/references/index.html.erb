<h1>Hello</h1>

<table>
	<tr>
		<th>Control Reference</th>
	</tr>
	<% @references.each do |ref|%>
	<tr>
		<td><%= link_to ref.full_ref, reference_path(ref) %></td>
	</tr>
	<% end %>
</table>
<%= paginate @references %>
